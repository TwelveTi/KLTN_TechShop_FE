import { PlannedSection } from '../components/PlannedSection'

/** Danh sách module dự kiến — màn hình tĩnh, có URL riêng `/admin/planned`. */
export function PlannedScreen() {
  return <PlannedSection />
}

export default PlannedScreen

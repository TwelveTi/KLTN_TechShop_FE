# TechShop AI Design Kit

Reference these files before generating UI.

Recommended prompt:

Read every markdown file inside `.ai/`.
Follow them strictly before writing any React code.
If a generated UI violates the anti-pattern rules, redesign it before responding.
